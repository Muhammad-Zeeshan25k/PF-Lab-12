#include <stdio.h>
int digitalroot(int n) {
    if (n < 10) 
        return n;

    int dr = 0; 
    while (n > 0) {
        dr += n % 10;
        n /= 10;
    }

    return digitalroot(dr); 
}
int main() {
    int a=9875;
    printf("%d",digitalroot(a));
    return 0;
}
